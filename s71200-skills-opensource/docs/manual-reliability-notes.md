# 澄清清单（阶段 1.5 必须处理的不一致与证据问题）

> 由阶段 1 各提取器 + 阶段 0 侦察报回汇总。**阶段 1.5 验证时逐条处置**：要么在 B（Boundary）中写明适用口径，要么转 `needs_review`。

## A. 手册内部自相矛盾（同一问题两种口径）

| # | 矛盾内容 | 位置 | 对 skill 的影响 |
|---|---|---|---|
| A1 | 安全实践要求"请勿启用 Anonymous 用户"，而 PUT/GET 远程访问步骤①恰恰要求先启用 Anonymous | p136/p139 vs p141 | 两条操作流程互斥，必须给取舍判据 |
| A2 | 铭牌标志只覆盖 2000 m，表格 A-9/A-10 允许 5000 m（A-9/A-10 还要求 FS 版本不低于某值） | p995 vs p993–994 | 高海拔合规判定无唯一答案 |
| A3 | 数据块结构变更：p968 说"可添加到 DB 结构并在 RUN 下下载"，p974 说"不会覆盖 DB 的结构更改" | p968 vs p974 | RUN 下载能力边界矛盾 |
| A4 | 端口黑名单两套口径：p577 列被动端口 20/80/102/5001/34962-34964；p565 说"除 161/34962-34964 外均可" | p565 vs p577 | 防火墙放行清单不一致 |
| A5 | 冷端误差口径：SM 版脚注"**要添加**到表中误差值上"，SB 版脚注"**已包括**到本表误差中" | p1090 vs p1127 | 两表数字看似可比、实际不可比 |
| A6 | RTD 超范围最大值：SM 版 `+32767`，SB 版 `+32768`（越出 Int 范围，疑笔误） | p1096 vs p1132 | 边界值判定 |
| A7 | AWP_Enum_Ref 已"不再需要"（p729），但 p732 的 Name 子句说明仍把它列入 | p721/729/732 | 该命令是否存在无定论 |
| A8 | 设备状态诊断：声称与诊断视图"一致"，但示例显示 MODE 2 与 MODE 5 返回值完全相同 | p359 vs p361/365 | 无法区分"拔模块"与"组态不符" |
| A9 | 数据日志：概述推荐"持久日志文件"，参数表写"掉电丢记录" | p400 | 数据可靠性判断被误导 |
| A10 | NTP 风险定性两处不一致，且均无加固步骤 | p267 vs p328 | 安全建议不可执行 |
| A11 | 32767 至少对应四种情形（断路/上溢/诊断未关闭/模块级），对照表与解析方法分处两节 | p947–948 vs p977 | 按单一页排查会误判 |
| A12 | p636 PUT 限值表第 3/4 组把"发送"笔误写成"获取" | p636 | 限值套用错误 |
| A13 | S_CONV 值→字符串：正文称"不可使用指数表示法"，但示例表却输出 `Real +9123.456 → "xx+9.123456E+3"`（ENO=TRUE） | p274 vs p279 | 正文与示例互斥，转换行为无定论 |
| A14 | D_ACT_DP 在 80A1/80A6 下的"部分失败"会伪装成 `W#16#0000`（成功） | p307–311 | 只看 RET_VAL=0 会误判成功 |
| A15 | 循环上电：p455 主动要求"循环上电"以评估存储卡，而 p982 明确"不要循环上电" | p455 vs p982 | 同一动作两处相反指令 |
| A16 | OPC UA 安全策略默认值：**"无安全"策略默认开启**（另有两条 Basic256Sha256 系默认开） | p651–656（pC-45） | 默认态不安全，须主动收紧 |
| A17 | **AWP_Start_Fragment 签名两处不一致**：p721 汇总写 `[Type][ID]`（无 Mode），p729 正文写 `[Type][ID][Mode]` 并大段定义"可见/隐藏" | p721 vs p729 | 该命令参数个数无定论（**第二处 AWP 矛盾**，第一处是 AWP_Enum_Ref） |
| A18 | "仅允许 HTTPS"：p732 称为**默认设置**，p753 却把 http/https 并列，且手册四次以"建议"口吻要求启用 | p732 vs p753 | **疑似非默认**——若按 p732 认为是默认，会误判初始安全状态 |

## Z. 页码纠错记录（提取器互查结果，已回原文确认）

| # | 原记录 | 纠正 | 证据 |
|---|---|---|---|
| Z1 | "OPC UA 只支持 RSA 2048" 记在 **p505–506** | **应为 p479–480**（"12.5 支持的证书"，含推荐证书参数表） | p505–506 实为 `TCON_QDN_SEC` 连接描述结构表（含 ActivateSecureConn / TLSServerReqClientCert 等字段）。由反例提取器发现、我已回原文确认 |
| Z2 | D_ACT_DP 的 80A1/80A6 说明标注为 p307–311 全段 | **精确为 p310** | 反例提取器回原文核验 |
| Z3 | HSC"紊乱"标注为 11.1.3.2/11.1.3.5 小节文本缺失 | **正文与警告框在 p455–456 可读**（11.1.3.7/11.1.3.8/11.1.3.9） | 反例提取器回原文核验；BOOK_OVERVIEW 的"缺页"记述应下调为"部分可读" |
| Z4 | 数据日志 1000 条上限记作 p713/715 | **数据日志 = p713，用户文件 = p715**（两条不同） | 同上 |
| Z5 | 组态控制访问静默记为 **p120** | **应为 p121**（『运行期间的特性』节）；p120 只有控制数据记录字节表与插槽值编码 | RA-1 回原文确认 |

## A-note. 新发现的静默失败（任务清单未列，已收录为反例）

| # | 内容 | 位置 |
|---|---|---|
| S1 | 组态控制中标记为"不存在"的模块：写不生效、读恒返回 0、无诊断、状态恒"正常"、**全程不报错** | p120（xB-17） |
| S2 | RSA 4096 证书**吃掉 ≥60% 扫描周期**（性能静默劣化） | p479–480 |
| S3 | **MB_SERVER 默认 `QB_Count`/`IB_Read_Count` = 65535、`Start` = 0** → **开箱即把整个 Q 区开放给任意 Modbus 客户端**；而手册给出的安全措施只有"限制客户端 IP"一条 | p827–828 |
| S4 | **缺授权时 CPU 仍照常通信**：仅 `STATUS_x` 恒为 `0A90`、`LICENSED=FALSE`、ERROR LED 闪烁、诊断缓冲区循环写许可证条目 | p864 |
| S5 | **不配 `Data_Area_Array` 时请求直接落过程映像**（不是"没配就不暴露"） | p830 |

## A-note2. Modbus 章节的三处逐字矛盾与两处同名不同默认

| # | 内容 | 位置 |
|---|---|---|
| A19 | 表 14-72 参数表**连续三行输出说明全标成 `RED_ERR_S7`**（应为 RED_ERR_S7 / RED_ERR_DEV / TOT_COM_ERR），而同页表头行本身是对的 | p834 |
| A20 | `Line_PRE` **同名两个默认值**：RTU 默认 0，USS 默认 2 且要求手工改成 0——交叉使用时极易混淆 | p874 vs p804 |
| B18 | 表 14-67 的 `QB_Read_Count` 说明误写成"如果 QB_Count = 0…"（引用了错误的变量名） | p828 |
| B19 | 表 14-69 粘连数字已定位为 `400990` / `4011920`；**按同页正文算例（起始 40021、长度 100 字）正确上界应为 40120** | p829 |
| A21 | `MB_MODE` 表的数据长度列**两套数字并排**（标准寻址 1–2000 / 扩展寻址 1–1992）——非印刷错误，须按寻址模式取 | p819–820 |
| A22 | GETIO_PART：正文写"**没有出现错误 → ERROR = TRUE**"，与同页参数表（ERROR=0 表示无错误）口径相反 | p303 |
| A23 | 禁用自动协商的后果两说：一处写"强制全双工"，另一处写"半双工" | p490 |
| A24 | CREATE_DB 源区小于目标区：p415 写"0 填充"，p417 写"保持不变" | p415 vs p417 |

## B-x. 手册"给不出数值"的诚实缺口（提取器已核实，非漏读）

| # | 内容 | 位置 | 核实结论 |
|---|---|---|---|
| N1 | PROFINET IO 循环更新时间档位数值 | p585 | **正文未列档位数值**，表 12-59 是对话框截图，本手册内无法引用数值清单 |
| N2 | MRP 看门狗时间的直接数值 | p608/610–612 | 只能经"更新周期数"间接设定，**手册不给换算公式** |
| N3 | 软件计数器速率 | p191 | 定性给"受 OB 执行速率限制"，**无公式** |
| N4 | ENDIS_PW 延迟时长推荐值 | p246 | 只说"用定时器延迟"，**无推荐值** |
| N5 | 数据块"下载而不重新初始化"的预留区该多大 | p972–974 | 只给默认 100 字节，**无定量方法** |

## B. 手册自身缺陷（不可据此写代码）

| # | 缺陷 | 位置 |
|---|---|---|
| B1 | 表 9-103 标题误写 `LOWER_BOUND`（实为 `UPPER_BOUND`） | p231 |
| B2 | 章节标题误作 DeviceStates（实为 ModuleStates） | p364 |
| B3 | SCL 示例以 `}` 结尾；`_unit_in` / `UNIT` 拼写错误 | p212/218/261–262 |
| B4 | Modbus 保持寄存器寻址表"最大值"列数字连排（"400990""4011920"），p887/938 出现"400119"多一位；且与同页正文口径不一致 | p829/887/923/938 |
| B5 | 段落残缺且表号从 14-87 跳至 14-95 | p884 |
| B6 | 同一参数默认值出现 3.0 与 .5 两种表述 | p934 |
| B7 | 复位为出厂设置第 3 步措辞含混 | p954 |
| B8 | p884 后 13.4/13.5 两级编号无标题行 | p689 后 |
| B9 | "S7-1200 G2"字样疑似模板复用错误 | p54 |
| B10 | 订货物号缺位（"6ES 278-4BD32"应为 `6ES7278-4BD32`） | p1102 |
| B11 | 表 C-3 标题误译：SB 写成"显示模块"、BB 写成"电池模块" | p1157 |
| B12 | 字段名串字 "DaylightStartWeekif"（应为 `DaylightStartWeek`） | p270 |
| B13 | 错误码语义重复：`2538/9528` ≡ `2550/9552`、`253D/9533` ≡ `253F/9535` | p250 表 9-153 |
| B14 | 表 10-33 同一输入 `"123.45"` + FORMAT 0000 出现两个不同 OUT 值（排版粘连，须以正文千位分隔符规则为准） | p279 |
| B15 | 拼写缺字 `GeInstancePath`（应为 `GetInstancePath`） | p295 |
| B20 | 报错表 `16#80B0` 说明写"控制数据记录的块类型（**字节 2**）不等于 196"，但记录结构表写**字节 1 = 块 ID 196、字节 2 = 版本 5** → **两页自相矛盾**（候选 C-452 照抄了原文，已标注口径不可依赖） | p122 vs p120 |
| B21 | 固件更新路径第 1 步把待删文件写成 **`S7_JOB.SYS`**，而 p102/104/107 均写 **`S7_JOB.S7S`**（候选 C-440 正确采用了 .S7S） | p109 |
| B22 | 表 10-69（FIND）的 WString 非法长度写作 **65535**，而同批其它指令表（CONCAT/LEFT/MID/DELETE/INSERT/REPLACE）一致写 **65534** → 口径不一致，引用时须按各表取 | p289 |

## A-note3. 候选规则本身的错误（验证阶段纠正，务必不要写进能力卡）

| # | 候选的错误表述 | 实读纠正 |
|---|---|---|
| R1 | C-649 称 `"3.4e39" → 3.4`（不会变 INF） | **仅对 `STRG_VAL` FORMAT 0000/0001（定点）成立**；FORMAT 0002/0003（指数）下 e/E 合法，`"3.4e39"`→**+INF**、`"-3.4e39"`→**−INF**（p279 表 10-33 实证）。**能力卡必须带 FORMAT 维度，否则是错误规则** |
| R2 | C-623 称错误表"27 条" | 实为 **23 条** |
| R3 | C-642 称"14 条错误码" | 实为 **13 条**，且 `0000`/`0001` 属正常态非错误 |
| R4 | C-635 称"范围求模" | **仅属 DECO**，不属 ENCO |
| R5 | C-587 缺第三条前置条件 | 漏"存储空间" |
| R6 | C-588 漏两条约束 | 禁多维 `Array of Bool`；OUT 仅 8/16/32/64 位 |
| R7 | C-856 称"五步通用" | HMI 5 步 / **PLC-PLC 实为 6 步** |
| R8 | C-852 称"ADDR 起始值 1" | 实为 `IF_CONF_V4` 的 **Id 起始值 30**，且 ADDR 是元素非独立子字段 |
| B16 | SB 1231 RTD 选型表 Pt100 气候型行**量程上下限颠倒**（上限 −145 < 下限 −120） | pE-17 对应页 |
| B17 | SB RTD 更新时间表 50 Hz 3 线制值 1.086 s，**破坏"3 线 = 4/2 线两倍"规律**（应为 0.132） | pE-18 对应页 |

## B-y. 手册不自足的领域（非外链，是整块缺失）

| # | 内容 | 证据 |
|---|---|---|
| H1 | **附录 A 技术规范 A.1–A.22 完全不含故障安全（F）模块规格** | F-CPU/SM 1226 仅出现在附录 C 订购表与 D 端子排表；手册对 F 系列规格不自足 |
| H2 | **SB/CB/BB 三者互斥共用一个板槽**（最多 1 个）——比"最多 1 个"更严格：装了 BB 1297 就不能装 SB | pE-22 |
| H3 | 安全程序嵌套深度为 **4**（16/6 之外的第三档） | pE-38 |

## C. 已复核的"疑似信息损失"（结论：可抢救，不必脑补）

| # | 原判 | 复核结论 |
|---|---|---|
| C1 | fB2-05 判定 p945–946 的 CPU 三灯对照表为"图形化表格，文本抽取仅得残片，须回原书核对" | **复核：可抢救**。JSONL 内 p945–p946 完整包含表格 15-1 全 11 行（断电/启动自检/停止/运行/取出存储卡/错误/请求维护/硬件故障/LED测试/组态版本未知，四列：STOP-RUN·ERROR·MAINT）与表格 15-2 开头。属**行结构被拉平**而非文本丢失。**task-137 可独立完成** |
| C2 | fA2 判定 task-49（计数器选型）"p190–192 未见计数器正文"，标待核查 | **复核：页码正确**。p190 = "9.3 计数器操作"，CTU/CTD/CTUD 与 R/LD 引脚定义；p191 = 存储占用 3/6/12 字节与"软件计数器速率受 OB 执行速率限制"；p192 续。属提取器批次边界误判 |
| C3 | pA2 判定 task-51/52（沿指令 M_BIT、首次扫描误沿）"未命中" | **复核：页码正确**。p182 = "9.1.3 上升沿和下降沿指令"（表 9-14）；p183 = P_TRIG/N_TRIG 与 **LAD 中不得放在程序段开头或结尾**规则；p184 续含 M_BIT 载体要求。属该 agent 读到但未识别为候选 |

> 结论：阶段 1 提取器报的"待核查"多为**批次边界误判**，不是任务清单错误。阶段 1.5 应要求验证者**直接回原文取页**，不采信提取器的自评。

## D. 外链缺口（手册不给，须外部文档）

| # | 内容 | 位置 |
|---|---|---|
| D1 | 运动控制轴组态/调试/回原点/ErrorID 全表 | p461 → 运动控制手册 |
| D2 | PID 整定步骤、采样时间、参数初值 | p468–471 → PID 功能手册 (108210036) |
| D3 | CP 1243 系列的连接/要求/应用/块说明/组态示例 | p944 → 三本 CP 操作说明 |
| D4 | 3964(R) 协议行为 | p759 → S7-300 CP 341 手册 |
| D5 | USS 结构要求与扩展错误码 | p906/807/810/913 → 驱动器手册 |
| D6 | Web API 完整定义（S7-1200 只覆盖 S7-1500 子集） | p752 → S7-1500 Web 服务器文档 |
| D7 | TINFO/AINFO 完整语义（AINFO 已由 fB2-13 补上字节 26–33 解析） | p306/312/313 |
| D8 | RDREC/WRREC 记录号含义 | p300 → 设备制造商文档 |
| D9 | S7 路由 CP 型号清单；OPC UA 方法参数；OPC UA Micro-Embedded Profile；低版本 OPC UA 用户验证 | p615/675/646/656 |
| D10 | 轨迹功能的组态与显示 | p975–977 → STEP 7 信息系统 + 外部在线手册 |
| D11 | 浪涌保护器件的"怎么装才有效" | p989 → 器件厂商说明书 |
| D12 | CSV 区域格式转换（USA/UK → 本地） | p713/754–755 |

## E. 阶段 1.5 处置要求

1. A 类矛盾：**不得静默任选一边**。能给出取舍判据的写入该能力的 B 字段；给不出判据的转 `needs_review` 并记录冲突定位。
2. B 类缺陷：任何引用了这些页面的候选，其 `source_quote` 必须核对，**不得把手册的错误原样写进能力卡**。
3. C1：按"可抢救"处理，验证时直接以 p945–946 的表格内容为准。
4. D 类：写入能力的 B 字段（"本条依赖外部文档 X，本手册不闭环"），并计入覆盖审计的缺口栏。
